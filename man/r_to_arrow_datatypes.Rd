% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-arrow-types.R
\name{r_to_arrow_datatypes}
\alias{r_to_arrow_datatypes}
\title{Create R type to Arrow DataType mapping}
\usage{
r_to_arrow_datatypes()
}
\value{
A named list with 6 entries mapping R types to Arrow DataType objects:
\describe{
\item{logical}{\code{arrow::bool()}}
\item{integer}{\code{arrow::int32()} (uses int32 as default)}
\item{double}{\code{arrow::float64()}}
\item{character}{\code{arrow::utf8()}}
\item{Date}{\code{arrow::date32()}}
\item{POSIXct}{\code{arrow::timestamp(unit = "ms")}}
}
}
\description{
Returns a named list mapping base R type strings (e.g., \code{"character"}, \code{"integer"})
to their corresponding Arrow \link[arrow:DataType-class]{arrow::DataType} objects. This is the inverse
of \link{arrow_to_r_datatypes} and is useful when creating Arrow schemas
programmatically from R type specifications.
}
\details{
This function generates the mapping dynamically. The R type
strings match those used in the \code{non_task_id_schema} field of \code{target-data.json}
configuration files.

This is particularly useful for:
\itemize{
\item Creating custom Arrow schemas from R type specifications
\item Converting configuration-based type information to Arrow schemas
\item Programmatic schema generation
}
}
\examples{
# Get the mapping
type_map <- r_to_arrow_datatypes()

# Use it to create Arrow types from R type strings
r_types <- c("character", "integer", "double")
arrow_types <- type_map[r_types]

# Create a simple Arrow schema
my_schema <- arrow::schema(
  name = type_map[["character"]],
  age = type_map[["integer"]],
  score = type_map[["double"]]
)
my_schema
}
\seealso{
\link{arrow_to_r_datatypes}, \code{\link[=create_timeseries_schema]{create_timeseries_schema()}}, \code{\link[=create_oracle_output_schema]{create_oracle_output_schema()}}
}
